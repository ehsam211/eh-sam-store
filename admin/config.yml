backend:
  name: git-gateway
  branch: main

media_folder: "images"
public_folder: "/images"

collections:
  - name: "products"
    label: "Store Products"
    folder: "products"
    create: true       
    delete: true       
    slug: "{{slug}}"
    format: "json"     
    extension: "json"   # <-- ADD THIS LINE to force .json files
    fields:
      - { label: "Product Name", name: "title", widget: "string" }
      - { label: "Price (₦ or $)", name: "price", widget: "string" }
      - { 
          label: "Category", 
          name: "category", 
          widget: "select", 
          options: ["Fashion", "Accessories", "Technology", "Lifestyle"] 
        }
      - { label: "Product Image", name: "image", widget: "image" }
      - { label: "Description", name: "description", widget: "text" }
      - { label: "Affiliate Link (Optional)", name: "affiliate_link", widget: "string", required: false }
      - { label: "In Stock", name: "in_stock", widget: "boolean", default: true }